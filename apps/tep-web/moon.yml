language: typescript
layer: application
stack: frontend

toolchain:
  default: bun

tags:
  - react-router
  - docker

project:
  name: tep-web
  description: "The web application for TEP built via React Router (Remix)"
  maintainers:
    - philippleonbusch@gmail.com
    - jabusch24

tasks:
  docker/build:
    deps:
      - build
